package com.chart;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.swing.DefaultBoundedRangeModel;

import org.jfree.data.general.DefaultPieDataset;
/*
 * 
 * 차트를 작성하는 순서
 *  1. 데이터를 생성한다.
 * 	 - 차트에 사용할 데이터는 Dataset 인터페이스를 구현한 클래스를 통해서 관리한다.
 * 		각 차트에 사용될 Dataset 클래스들이 서로 달라지기 때문에 
 * 		원하는 Dataset 클래스를 생성한다.
 * 
 * 	2. 차트를 만든다.
 * 	 - 차트를 생성하기 위해서는 ChartFactory클래스의 createXXXChart() 형태의
 * 		정적 메소드를 사용한다.
 * 
 *  3. 차트를 원하는 형태로 만든다.
 *  
 *  4. 차트를 출력한다.
 *  	- LocalApplication에서는 ChartFrame 클래스를 이용하여 프레임을 생성해서
 *  		보여주고 Web Application에서는 ServletOutputStream 클래스와
 *  		ChartUtilities 클래스를 이용하여 브라우저에 출력함
 *  	
 *  		Local Application
 *  			ChartFrame frame = new ChartFrame("First",chart);
 *  			frame.pack();
 *  			frame.setVisible(true);
 *  		Web Application
 *  			ServletOutputStream out = response.getOurpurStrea();
 * 				response.setContentType("image/png");
 * 				ChartUtilites.writeChartAsPNG(out,chart,width,height);
 * 		
 * 
 * 
 * */
@WebServlet("/PieChart")
public class PieChartServlet extends HttpServlet {
	private static final long serialVersionUID = 1L;

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

		//1. 데이터 생성
		DefaultPieDataset
		//2. 차트를 만듬
		
		//3. 차트를 출력
		
	
	}

}
